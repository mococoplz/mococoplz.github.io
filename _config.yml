# Hexo Configuration
## Docs: https://hexo.io/docs/configuration.html
## Source: https://github.com/hexojs/hexo/

# Site
title: Hexo
subtitle: ''
description: ''
keywords:
author: John Doe
language: zh-CN


timezone: ''

# URL
## Set your site url here. For example, if you use GitHub Page, set url as 'https://username.github.io/project'
url: http://example.com
permalink: :year/:month/:day/:title/
permalink_defaults:
pretty_urls:
  trailing_index: true # Set to false to remove trailing 'index.html' from permalinks
  trailing_html: true # Set to false to remove trailing '.html' from permalinks

# Directory
source_dir: source
public_dir: public
tag_dir: tags
archive_dir: archives
category_dir: categories
code_dir: downloads/code
i18n_dir: :lang
skip_render:

# Writing
new_post_name: :title.md # File name of new posts
default_layout: post
titlecase: false # Transform title into titlecase
external_link:
  enable: true # Open external links in new tab
  field: site # Apply to the whole site
  exclude: ''
filename_case: 0
render_drafts: false
post_asset_folder: false
relative_link: false
future: true
syntax_highlighter: prismjs
highlight:
  enable: true
  theme:
    light: prism      # 使用 Prism 浅色主题
    dark: prism-dark  # 使用 Prism 深色主题
  line_number: true       # 显示代码行号
  auto_detect: false      # 禁用自动主题检测
  tab_replace: ''         # 替换制表符为空
  wrap: true              # 包裹代码块
  hljs: false             # 禁用 highlight.js，使用 Prism

  light:
    background: "#ffffff" # 浅色主题背景色
    foreground: "#000000" # 浅色主题前景色（文本颜色）

  dark:
    background: "#2e2e2e" # 深色主题背景色
    foreground: "#f1f1f1" # 深色主题前景色（文本颜色）

prismjs:
  enable: true       # 启用 Prism.js
  preprocess: true   # 启用预处理
  line_number: true  # 启用代码行号
  line_threshold: 0  # 设置行数阈值
  tab_replace: ''    # 替换制表符的设置

# Home page setting
# path: Root path for your blogs index page. (default = '')
# per_page: Posts displayed per page. (0 = disable pagination)
# order_by: Posts order. (Order by date descending by default)
index_generator:
  path: ''
  per_page: 10
  order_by: -date

# Category & Tag
default_category: uncategorized
category_map:
tag_map:

# Metadata elements
## https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meta
meta_generator: true

# Date / Time format
## Hexo uses Moment.js to parse and display date
## You can customize the date format as defined in
## http://momentjs.com/docs/#/displaying/format/
date_format: YYYY-MM-DD
time_format: HH:mm:ss
## updated_option supports 'mtime', 'date', 'empty'
updated_option: 'mtime'

# Pagination
## Set per_page to 0 to disable pagination
per_page: 10
pagination_dir: page

# Include / Exclude file(s)
## include:/exclude: options only apply to the 'source/' folder
include:
exclude:
ignore:


# Extensions
## Plugins: https://hexo.io/plugins/
## Themes: https://hexo.io/themes/
theme: next

# Deployment
## Docs: https://hexo.io/docs/one-command-deployment
deploy:
  type: git
  repository: git@github.com:mococoplz/mococoplz.github.io
  branch: gh-pages  # 或者 'gh-pages'，根据你的 GitHub Pages 配置
prism_plugin:
  enable: true         # 启用 Prism 插件
  preprocess: true     # 启用预处理功能
  line_number: true    # 显示行号
  line_threshold: 0    # 设置代码行数阈值
  tab_replace: '    '  # 替换制表符为空格
